package com.hanpf.clearai.react.agent;

/**
 * ReactAgent - 核心ReAct代理（暂时简化版本）
 * 负责处理用户的路径扫描请求，制定扫描策略
 */
public class ReactAgent {

    /**
     * 处理用户请求（暂时返回空字符串，避免编译错误）
     */
    public String processUserRequest(String userInput) {
        return "";
    }
}